{{#each posts as |post| }}
<article>
    <h2><a href="/post/{{post.id}}">{{post.title}}</a></h2>
    <small>Posted by {{post.user.name}} on {{format_date post.date_created}}</small>
</article>
{{/each}}
