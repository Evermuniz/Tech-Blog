<div class="text-center">
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>Created by {{post.created_by}} on {{post.date_created}}</p>
    <p>
        <button class="btn btn-primary">Fund this Project!</button>
    </p>
</div>